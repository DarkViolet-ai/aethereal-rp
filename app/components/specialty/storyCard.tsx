import Flex from "../buildingBlocks/flex";

interface StoryCard {
    title: string;
    summary: string;
    image: string;
    link: string;
}
export default function StoryCard() {
  return <Flex className="">

  </Flex>;
}
